<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AsyncTask" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/ijonom</item>
        <item name="colorPrimaryDark">@color/kuneng</item>
    </style>

    <style name="Theme.AsyncTask" parent="Base.Theme.AsyncTask" />
</resources>